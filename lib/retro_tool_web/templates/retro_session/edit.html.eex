<h2>Edit Retro session</h2>

<%= render "form.html", Map.put(assigns, :action, retro_session_path(@conn, :update, @retro_session)) %>

<span><%= link "Back", to: retro_session_path(@conn, :index) %></span>
